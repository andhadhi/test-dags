{
  "name": "scheduled-cleanup",
  "operator": "kubernetes",
  "description": "Scheduled job that runs every hour to cleanup old files",
  "cron": "0 * * * *",
  "params": {},
  "tasks": [
    {
      "name": "cleanup-temp",
      "depends_on": [],
      "container": {
        "image": "node:22-alpine",
        "command": ["node", "-e", "console.log('Cleanup started at:', new Date().toISOString()); console.log('Cleaning temp files...'); setTimeout(() => console.log('Cleanup complete!'), 2000);"]
      }
    }
  ]
}
